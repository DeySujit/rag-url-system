# ---- core framework ----
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3

# ---- crawling / extraction ----
crawl4ai>=0.4.0           # primary crawler (Playwright-based)
httpx>=0.27               # async fallback fetcher
beautifulsoup4>=4.12
lxml>=5.2

# ---- chunking ----
langchain-text-splitters>=0.2
tiktoken>=0.7

# ---- embeddings / llm ----
# Default embedding backend (OpenAI) + chat LLM. Install extra embedding
# providers only if you set EMBEDDING_BACKEND to one of them:
openai>=1.40                # EMBEDDING_BACKEND=openai (default)
google-genai>=1.0         # EMBEDDING_BACKEND=gemini
voyageai>=0.3             # EMBEDDING_BACKEND=voyage
cohere>=5.13              # EMBEDDING_BACKEND=cohere
tenacity>=8.3

# ---- vector store: default backend (pgvector via Postgres) ----
asyncpg>=0.29
sqlalchemy[asyncio]>=2.0
psycopg[binary]>=3.1      # sync driver for migrations

# ---- logging ----
loguru>=0.7

# ---- optional vector backends (install only what you use) ----
# pinecone-client>=5.0
# qdrant-client>=1.10
# weaviate-client>=4.6
# pymilvus>=2.4

# ---- dev / test ----
pytest>=8.2
pytest-asyncio>=0.23
